{
  "name": "erp-sales-proxy",
  "version": "1.0.0",
  "scripts": {
    "client": "cd react-src && yarn start",
    "server": "nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"yarn server\" \"yarn client\""
  },
  "dependencies": {
    "axios": "^0.16.2",
    "express": "^4.16.2",
    "request": "^2.85.0"
  },
  "devDependencies": {
    "concurrently": "^3.5.0"
  }
}
